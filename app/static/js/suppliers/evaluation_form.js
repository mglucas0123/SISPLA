(function (window, document) {
    function init(options = {}) {
        console.debug('Suppliers evaluation form module initialized', options);
    }

    window.SuppliersEvaluationForm = { init };
})(window, document);
