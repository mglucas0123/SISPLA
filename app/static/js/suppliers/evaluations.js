(function (window, document) {
    function init(options = {}) {
        console.debug('Suppliers evaluations module initialized', options);
    }

    window.SuppliersEvaluations = { init };
})(window, document);
